# Environment variables for Science Paper Summariser
# Copy this file to .env and fill in your API keys as needed.
#
# API keys are ONLY required when using api mode with the corresponding provider.
# CLI providers (claude, codex, gemini, copilot, opencode) and local API providers
# (ollama, openai-compatible) do not require any keys in this file.

# Anthropic API key (api claude)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenAI API key (api openai)
OPENAI_API_KEY=your_openai_api_key_here

# Perplexity API key (api perplexity)
PERPLEXITY_API_KEY=your_perplexity_api_key_here

# Google API key (api gemini)
GOOGLE_API_KEY=your_google_api_key_here

# openai-compatible base URL (api openai-compatible) — required for that provider.
# Set this to the /v1 endpoint of your local inference server, e.g.:
#   LM Studio:  http://localhost:1234/v1
#   llama.cpp:  http://localhost:8080/v1
#   Ollama:     http://localhost:11434/v1
OPENAI_COMPATIBLE_BASE_URL=http://localhost:1234/v1

# openai-compatible key (optional): if your server requires authentication,
# set the key here and point api_key_env at this variable name in provider config.
# MY_LOCAL_API_KEY=your_key_here
